# Copy to .env and fill in locally. Never commit real keys.

# Optional: enables live AI concierge (otherwise RAG fallback is used)
OPENAI_API_KEY=

# Optional / reserved
# GOOGLE_API_KEY=

# Optional DB override (Docker compose sets this)
# DATABASE_URL=sqlite:////app/data/mare_arabia.db
